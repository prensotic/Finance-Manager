export class Card{
  constructor(id, userId, typeCard, number, balance){
    this.Id = id;
    this.UserId = userId;
    this.TypeCard = typeCard;
    this.Number = number;
    this.Balance = balance;
  }
}